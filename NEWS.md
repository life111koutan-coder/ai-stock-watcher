# AIニュース監視

既存の価格・仮想売買・ランキングとは独立したニュース欄。
国内監視リストは `watchlist.json`、分野と企業名別名は `news_sources.json`。
海外AI企業の情報も収集するが、銘柄の関連付けは国内監視リストのみ。

## 更新と限界

- Google News RSSを5検索、各回並列取得。過去7日・最大300件の見出しとリンクのみ保存。
- 毎時7・22・37・52分、土日夜間も実行予定。GitHub Actionsと検索インデックスの遅延があるためリアルタイム保証なし。
- ブラウザーは5分ごとに保存データを再読込。巡回が1時間以上古い場合は警告。
- 配信元ごとの失敗と最終成功を表示。失敗時は期限内の既存記事を保持し、成功と偽らない。
- 企業公式ドメインの配信元は「企業公式」、それ以外は「報道・その他」。公式サイトの直接巡回・TDnet完全収集ではない。
- 見出しでAI等の分野を抽出。見出しに企業名がある場合だけ監視銘柄に関連付ける。取引関係や受益を推測しない。
- 重要度はキーワード規則。本文は取得しない。生成AI本文分析・業績影響予測・自動売買ではない。追加の有料AI APIは使用しない。
- 価格スコアは既存の株価ルールによる値。ニュースにより変更しない。
- Googleの配信仕様変更やアクセス制限の影響を受ける。取得元設定の定期的な保守が必要。

## LINE（既定では無効）

既存watch.pyのbroadcastを再利用せず、本人専用のpushだけ実装。
本人の宛先を確認し、GitHub Actions secret `LINE_NEWS_USER_ID` を設定してから
repository variable `NEWS_LINE_ENABLED` を `true` にする。
既存secret `LINE_CHANNEL_ACCESS_TOKEN` を利用。秘密値・宛先をコードや公開JSONに入れない。
宛先の確認前には有効化しない。LINEの配信枠・料金は契約による。

初回は既存記事を通知しない。以後6時間以内の新着で「企業公式」かつ「重要」の
記事を1回最大3件にまとめる（超過分は画面で確認）。送信失敗時は次回再試行。
送信後の保存失敗では重複通知の可能性がある（厳密なexactly-onceではない）。
初回手元実行は `python3 news.py`（通知しない）。

## 検証

`python3 -m unittest discover -s tests -p 'test_news.py'`

スクリプトはPython標準ライブラリのみ。RSS失敗時も状態JSONを保存する。
Actionsの成功だけで正常判定せず、画面の取得状況も見る。
